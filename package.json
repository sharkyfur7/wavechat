{
  "private": true,
  "workspaces": [
    "shared",
    "frontend",
    "backend"
  ],
  "dependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "cd shared && npm run build && cd .. && concurrently 'cd backend && npm run dev' 'cd frontend && npm run dev'"
  }
}
